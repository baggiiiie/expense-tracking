{
  "name": "expense-tracking-flue",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Flue agents for the expense-tracking project (GitHub issue automation).",
  "scripts": {
    "eval": "vitest run --config .flue/evals/vitest.config.ts",
    "resolve-issue": "node .flue/scripts/resolve-issue.mjs",
    "test": "node --test .flue/tests/resolve-issue.test.ts",
    "typecheck": "tsc --noEmit",
    "verify": "npm run verify:web && npm run verify:server",
    "verify:web": "cd server/web && pnpm install --frozen-lockfile && pnpm build",
    "verify:server": "cd server && go build ./... && go test ./..."
  },
  "dependencies": {
    "@earendil-works/pi-ai": "^0.83.0",
    "@flue/runtime": "^2.0.2",
    "valibot": "latest"
  },
  "devDependencies": {
    "@flue/cli": "^2.0.2",
    "agent-browser": "0.32.4",
    "typescript": "latest",
    "vitest": "^4.1.10"
  }
}
